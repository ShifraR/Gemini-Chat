# 🤖 Gemini AI Chat (NetFree Friendly)

צ'אטבוט אינטראקטיבי המבוסס על מודל ה-AI של גוגל (**Gemini**), עם ממשק משתמש מודרני מבוסס **Streamlit**. 
הפרויקט הותאם במיוחד לעבודה חלקה בעברית (RTL) ובסביבת האינטרנט של **נטפרי**.

## ✨ תכונות עיקריות
* **ממשק עברי מלא:** יישור לימין (Right-to-Left) בבועות הצ'אט ובשדה הקלט.
* **ניהול זיכרון:** הבוט זוכר את תוכן השיחה הקודם ומגיב בהתאם להקשר.
* **תאימות לנטפרי:** הגדרה אוטומטית של תעודות אבטחה (SSL) והודעת חסימה מותאמת אישית.
* **עיצוב נקי:** כולל "גלגל חושב" בזמן המתנה לתשובה וכפתור לניקוי היסטוריה.

---

## 🚀 איך מריצים את הפרויקט?

### 1. הכנת הסביבה (בטרמינל)
פתחו את תיקיית הפרויקט בטרמינל והריצו את הפקודות הבאות:
```bash
# יצירת סביבה וירטואלית
python -m venv venv

# הפעלת הסביבה (Windows)
.\venv\Scripts\activate

# התקנת הספריות הדרושות
pip install -r requirements.txt

### 2. הגדרת מפתח ה-API (API Key)
כדי שהצ'אט יוכל להתחבר למודל של גוגל, עליכם להגדיר את המפתח האישי שלכם:
1. בתיקיית הפרויקט תמצאו קובץ בשם `.env`.
2. פתחו אותו והחליפו את הטקסט `your_api_key_here` במפתח שקיבלתם מגוגל.
   * דוגמה: `GOOGLE_API_KEY=AIzaSyA123...`
3. שמרו את הקובץ.

### 3. הרצת האפליקציה
כדי להפעיל את ממשק הצ'אט בדפדפן, הריצו בטרמינל את הפקודה:
```bash
streamlit run web_app.py

הדפדפן ייפתח אוטומטית בכתובת http://localhost:8501


🔑 איך מקבלים מפתח API? (מדריך למתחילים)
השימוש ב-Gemini API הוא בחינם (במגבלות מסוימות). כך תנפיקו מפתח משלכם:

היכנסו לאתר Google AI Studio.

התחברו עם חשבון הגוגל (Gmail) שלכם.

בתפריט הצדדי (משמאל למעלה), לחצו על הכפתור הכחול "Get API key".

לחצו על הכפתור "Create API key in new project".

המערכת תייצר לכם קוד ארוך שמתחיל ב-AIza.... העתיקו אותו.

זהו המפתח שלכם! הדביקו אותו בקובץ ה-.env כפי שהוסבר למעלה.

🛡️ הערות למשתמשי נטפרי
הפרויקט מותאם במיוחד לעבודה חלקה בסביבת נטפרי:

תעודת אבטחה (SSL): הקוד מוגדר לחפש אוטומטית את תעודת ה-CA של נטפרי בנתיב:
C:\ProgramData\NetFree\CA\netfree-ca-bundle-curl.crt.
אם התעודה מותקנת בנתיב אחר, יש לעדכן את המשתנה netfree_path בקובץ ה-Python.

טיפול בחסימת תוכן: במקרה שנטפרי חוסמת שאילתה מסוימת (שגיאה 418), האפליקציה תציג הודעה מפורשת:

"אופסס... נטפרי לא מרשה לי לדבר איתך על זה, לפרטים נוספים עיינו בקישור הזה..."

תהנו מהשימוש בצ'אט!
